# D UI library

## Publish
1. Test, commit and push changes
2. Update lib version `npm version major | minor | patch`
3. Run `npm publish`

## Alternative publish
1. Test, commit and push changes
2. Run `npm run pub`